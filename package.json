{
  "name": "oa-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web-app && npm run build",
    "dev": "cd web-app && npm run dev",
    "start": "cd web-app && npm start"
  }
}

