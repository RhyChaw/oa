
LLM_MODEL=llama3.1

OLLAMA_BASE_URL=http://localhost:11434
MAX_TOKENS=1024
TEMPERATURE=0.7

postgresql://leet:coach@localhost:5432/leetcode
EMBED_DIM=1536
# The host and port for the FastAPI agent
API_HOST=0.0.0.0
API_PORT=8000
AGENT_ROLE=LeetCode Problem Helper

# Default programming language context (overridden by user selection)
DEFAULT_LANG=python


# Whether to persist chat memory between sessions (true/false)
PERSIST_MEMORY=true

# Path where chat memory is stored (mounted inside container)
MEMORY_PATH=/app/memory/chat_history.json